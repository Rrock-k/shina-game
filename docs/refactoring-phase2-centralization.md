# План рефакторинга: Централизация управления игрой

## 🎯 Цель
Завершить рефакторинг, перенеся всю логику управления, состоянием и игровым циклом из `main.js` в новый класс `Game.js`, как это было запланировано в `docs/final-architecture.md`. `main.js` должен стать тонкой точкой входа в приложение.

## Этап 1: Создание фундамента (Критично)

### 1.1 Создать класс Game.js ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Создать базовую структуру класса Game с конструктором и пустыми методами `start()` и `update(delta)`
- **Файлы**: `src/game/Game.js` (новый файл)
- **Проверка**: Класс Game создан и может быть импортирован без ошибок.

### 1.2 Импортировать Game.js в main.js ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Импортировать класс Game в `main.js` и создать его экземпляр `const game = new Game();`
- **Файлы**: `src/main.js`
- **Проверка**: Проект запускается без ошибок. В консоли можно получить доступ к пустому объекту game.

## Этап 2: Централизация инициализации (Критично)

### 2.1 Перенести создание PIXI приложения ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Перенести только код из функции `setupApp()` в конструктор класса Game
- ✅ **ВЫПОЛНЕНО**: Глобальная переменная `app` становится свойством класса `this.app`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: PIXI canvas по-прежнему корректно рендерится. Приложение доступно как `this.app`.

### 2.2 Перенести создание слоев ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Перенести логику создания слоев (`gridLayer`, `roadsLayer` и т.д.) из `setupWorld()` в конструктор класса Game
- ✅ **ВЫПОЛНЕНО**: Глобальные переменные (`world`, `gridLayer`, `roadsLayer`, `decorLayer`, `zonesLayer`) становятся свойствами класса
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Все слои созданы и доступны как свойства экземпляра game.

### 2.3 Перенести создание менеджеров ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Перенести создание экземпляров менеджеров (`TimeManager`, `PauseManager`, `DayNightManager`, `JournalManager`) в конструктор `Game.js`
- ✅ **ВЫПОЛНЕНО**: Они становятся свойствами класса (`this.timeManager`, `this.pauseManager`, `this.dayNightManager`, `this.journalManager`)
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Все менеджеры успешно создаются и доступны через экземпляр game.

### 2.4 Перенести создание рендереров ✅ (ВЫПОЛНЕНО)
- ✅ **ВЫПОЛНЕНО**: Перенести создание рендереров (`WorldRenderer`, `UIRenderer`) в конструктор `Game.js`
- ✅ **ВЫПОЛНЕНО**: Они становятся свойствами класса (`this.worldRenderer`, `this.uiRenderer`)
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Рендереры создаются и доступны через экземпляр game. Часы на UI должны начать работать.

### 2.5 Перенести создание сущности Car ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести создание `carEntity` из `initEntities()` в конструктор `Game.js`
- ⏳ **ПЛАНИРУЕТСЯ**: `carEntity` становится `this.carEntity`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Машина появляется на карте в своей начальной позиции. Доступна как `this.carEntity`.

### 2.6 Перенести создание сущности Shina ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести создание `shinaEntity` из `initEntities()` в конструктор `Game.js`
- ⏳ **ПЛАНИРУЕТСЯ**: `shinaEntity` становится `this.shinaEntity`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Шина появляется на карте. Доступна как `this.shinaEntity`.

## Этап 3: Централизация игрового цикла (Критично)

### 3.1 Создать метод Game.update() ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Создать пустой метод `update(delta)` в классе Game
- ⏳ **ПЛАНИРУЕТСЯ**: Добавить базовую структуру метода с параметром delta
- **Файлы**: `src/game/Game.js`
- **Проверка**: Метод создан и может быть вызван без ошибок.

### 3.2 Перенести обновление времени ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести вызовы `timeManager.update(delta)` в метод `Game.update()`
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.timeManager`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Время в игре продолжает обновляться корректно.

### 3.3 Перенести обновление UI ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести вызовы `uiRenderer.update()` в метод `Game.update()`
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.uiRenderer`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: UI обновляется корректно, часы работают.

### 3.4 Перенести обновление машины ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести логику обновления машины из `updateCar()` в метод `Game.update()`
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к `carEntity` на `this.carEntity`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Машина продолжает двигаться и следовать по маршруту.

### 3.5 Перенести обновление смены дня/ночи ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Перенести вызовы `dayNightManager.update()` в метод `Game.update()`
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.dayNightManager`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Смена дня и ночи продолжает работать корректно.

### 3.6 Создать метод Game.start() ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: В классе Game создать метод `start()`, который будет содержать `this.app.ticker.add((delta) => this.update(delta));`
- **Файлы**: `src/game/Game.js`
- **Проверка**: Метод start() создан и может быть вызван.

### 3.7 Удалить app.ticker из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Удалить все вызовы `app.ticker.add(...)` из `main.js`
- **Файлы**: `src/main.js`
- **Проверка**: app.ticker удален из main.js, проект компилируется.

### 3.8 Вызвать game.start() в main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: В `main.js` вызвать `game.start()` для запуска игрового цикла
- **Файлы**: `src/main.js`
- **Проверка**: Игра запускается и работает как и прежде, подтверждая, что игровой цикл теперь полностью управляется классом Game.

## Этап 4: Перенос логики управления состоянием (Важно)

### 4.1 Перенести функцию nextDestination из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Скопировать функцию `nextDestination` из `main.js` в класс Game как приватный метод `_nextDestination`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Функция скопирована в класс Game.

### 4.2 Адаптировать функцию _nextDestination в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.` в методе `_nextDestination`
- **Файлы**: `src/game/Game.js`
- **Проверка**: Функция работает как приватный метод класса.

### 4.3 Перенести функцию checkArrival из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Скопировать функцию `checkArrival` из `main.js` в класс Game как приватный метод `_checkArrival`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Функция скопирована в класс Game.

### 4.4 Адаптировать функцию _checkArrival в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.` в методе `_checkArrival`
- **Файлы**: `src/game/Game.js`
- **Проверка**: Функция работает как приватный метод класса.

### 4.5 Перенести функцию updateStayTimer из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Скопировать функцию `updateStayTimer` из `main.js` в класс Game как приватный метод `_updateStayTimer`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Функция скопирована в класс Game.

### 4.6 Адаптировать функцию _updateStayTimer в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.` в методе `_updateStayTimer`
- **Файлы**: `src/game/Game.js`
- **Проверка**: Функция работает как приватный метод класса.

### 4.7 Перенести функции аватарок зданий из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Скопировать функции `showBuildingAvatar` и `hideBuildingAvatar` из `main.js` в класс Game как приватные методы
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Функции скопированы в класс Game.

### 4.8 Адаптировать функции аватарок в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.` в методах аватарок
- **Файлы**: `src/game/Game.js`
- **Проверка**: Функции работают как приватные методы класса.

### 4.9 Перенести функцию saveCarStateForNextDestination из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Скопировать функцию `saveCarStateForNextDestination` из `main.js` в класс Game как приватный метод `_saveCarStateForNextDestination`
- **Файлы**: `src/main.js` → `src/game/Game.js`
- **Проверка**: Функция скопирована в класс Game.

### 4.10 Адаптировать функцию _saveCarStateForNextDestination в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Заменить обращения к глобальным переменным на `this.` в методе `_saveCarStateForNextDestination`
- **Файлы**: `src/game/Game.js`
- **Проверка**: Функция работает как приватный метод класса.

### 4.11 Обновить вызовы функций в Game.update() ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Обновить все вызовы перенесенных функций в методе `Game.update()` на `this._methodName()`
- ⏳ **ПЛАНИРУЕТСЯ**: Убедиться, что все функции вызываются корректно
- **Файлы**: `src/game/Game.js`
- **Проверка**: Машина корректно следует по маршруту, останавливается в локациях, отображает аватарки в зданиях и продолжает движение по истечении таймера.

### 4.12 Удалить перенесенные функции из main.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Удалить все функции, которые были перенесены в класс Game, из `main.js`
- **Файлы**: `src/main.js`
- **Проверка**: Функции удалены из main.js, проект компилируется без ошибок.

## Этап 5: Очистка зависимостей (Важно)

### 5.1 Изменить DayNightManager для получения слоев через параметры ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Изменить конструктор `DayNightManager` для получения PIXI-контейнеров через параметры
- ⏳ **ПЛАНИРУЕТСЯ**: Убрать обращения к `window.world` и другим глобальным слоям
- **Файлы**: `src/game/DayNightManager.js`
- **Проверка**: Класс DayNightManager не использует глобальные переменные.

### 5.2 Обновить создание DayNightManager в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: В `Game.js`, при создании `dayNightManager`, передать ему `this.world`, `this.decorLayer` и т.д.
- ⏳ **ПЛАНИРУЕТСЯ**: Убедиться, что все необходимые слои передаются корректно
- **Файлы**: `src/game/Game.js`
- **Проверка**: DayNightManager создается с правильными параметрами.

### 5.3 Изменить UIRenderer для получения состояния машины через параметры ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Удалить использование `window.carEntity` в `UIRenderer`
- ⏳ **ПЛАНИРУЕТСЯ**: Изменить метод `update()` для получения состояния машины через параметр
- **Файлы**: `src/rendering/UIRenderer.js`
- **Проверка**: UIRenderer не использует глобальные переменные.

### 5.4 Обновить вызовы UIRenderer.update() в Game.js ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: В `Game.js`, при вызове `uiRenderer.update()`, передавать актуальное состояние машины
- ⏳ **ПЛАНИРУЕТСЯ**: Убедиться, что все необходимые данные передаются корректно
- **Файлы**: `src/game/Game.js`
- **Проверка**: UI (статус фар, информация о маршруте) продолжает корректно отображать состояние машины.

## Этап 6: Финализация (Критично)

### 6.1 Удалить глобальные переменные ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Удалить объявления глобальных переменных (`let app, world, ...`) из `main.js`
- ⏳ **ПЛАНИРУЕТСЯ**: Убедиться, что все переменные теперь доступны через экземпляр game
- **Файлы**: `src/main.js`
- **Проверка**: Глобальные переменные удалены, проект компилируется без ошибок.

### 6.2 Удалить перенесенные функции ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: Удалить все функции, которые были перенесены в класс Game
- ⏳ **ПЛАНИРУЕТСЯ**: Убедиться, что все функции теперь доступны как методы класса
- **Файлы**: `src/main.js`
- **Проверка**: Функции удалены, проект компилируется без ошибок.

### 6.3 Упростить main.js до точки входа ⏳ (ПЛАНИРУЕТСЯ)
- ⏳ **ПЛАНИРУЕТСЯ**: В файле должны остаться только импорты и код для создания и запуска игры: `const game = new Game(); game.start();`
- ⏳ **ПЛАНИРУЕТСЯ**: Убрать все остальные вызовы и инициализацию
- **Файлы**: `src/main.js`
- **Проверка**: Проект полностью функционален. Файл `main.js` содержит менее 10 строк кода.

## ⚠️ Риски и митигация

### 🔴 Высокий риск
- **Нарушение работы игрового цикла** - при переносе логики в `Game.update()` может сломаться синхронизация
  - *Митигация:* Тщательное тестирование после каждого изменения, сохранение бэкапа перед началом этапа 3
- **Потеря состояния приложения** - при переносе глобальных переменных в класс может нарушиться доступ к данным
  - *Митигация:* Пошаговая миграция с проверкой на каждом шаге, использование `console.log` для отслеживания состояния

### 🟡 Средний риск
- **Циклические зависимости** - при изменении `DayNightManager` и `UIRenderer` могут возникнуть циклические импорты
  - *Митигация:* Проверка зависимостей перед изменением, использование dependency injection
- **Производительность** - добавление слоя абстракции может повлиять на производительность
  - *Митигация:* Профилирование до и после изменений, оптимизация при необходимости

### 🟢 Низкий риск
- **Синтаксические ошибки** - при переносе кода могут появиться опечатки
  - *Митигация:* Использование IDE с автодополнением, линтер, тестирование после каждого изменения

## ✅ Чек-лист готовности

### Перед началом рефакторинга
- [ ] Изучена структура `main.js` 
- [ ] Понятны зависимости между компонентами

### После каждого этапа
- [ ] Проект компилируется без ошибок
- [ ] Игра запускается и работает корректно
- [ ] Все функции работают как прежде
- [ ] Нет ошибок в консоли браузера

### Этап 1 - Создание фундамента
- [x] Класс `Game.js` создан ✅
- [x] Методы `start()` и `update(delta)` добавлены ✅
- [x] Импорт в `main.js` работает ✅
- [x] Экземпляр `game` создается без ошибок ✅

### Этап 2 - Централизация инициализации
- [x] PIXI приложение создается в конструкторе Game ✅
- [x] Все слои доступны как свойства класса ✅
- [x] Менеджеры инициализируются в Game ✅
- [x] Рендереры создаются в Game ✅
- [ ] Сущности (Car, Shina) создаются в Game
- [ ] UI отображается корректно

### Этап 3 - Централизация игрового цикла
- [ ] Метод `update(delta)` содержит всю логику
- [ ] Глобальные переменные заменены на `this.`
- [ ] `app.ticker` вызывает `Game.update()`
- [ ] Движение машины работает
- [ ] Время обновляется
- [ ] Смена дня/ночи функционирует

### Этап 4 - Перенос логики управления состоянием
- [ ] Все функции перенесены в Game как приватные методы
- [ ] Вызовы обновлены на `this._methodName()`
- [ ] Маршрутизация работает
- [ ] Таймеры останова функционируют
- [ ] Аватарки зданий отображаются

### Этап 5 - Очистка зависимостей
- [ ] `DayNightManager` не использует `window`
- [ ] `UIRenderer` не использует `window.carEntity`
- [ ] Все зависимости передаются через параметры
- [ ] Ночной режим работает
- [ ] UI обновляется корректно

### Этап 6 - Финализация
- [ ] `main.js` содержит только импорты и запуск
- [ ] Все глобальные переменные удалены
- [ ] Файл `main.js` менее 10 строк
- [ ] Проект полностью функционален
- [ ] Код готов к продакшену

## Статистика прогресса
- **Всего задач**: 28
- **Выполнено**: 6 ✅
- **В процессе**: 0
- **Осталось**: 22

## Примечания
- Каждую выполненную задачу отмечать галочкой [x]
- При возникновении проблем добавлять комментарии к задаче
- Обновлять статистику прогресса после завершения каждой задачи
- Следовать принципу пошаговой миграции с проверкой на каждом этапе
