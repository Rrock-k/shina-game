# Карта Шины — заметки по текущему прогрессу

Дата: 2024‑12‑19

## Что сделано в последней сессии
- [x] Очистили репозиторий: убраны архивы, `node_modules`, временные файлы; документация пересобрана под актуальное состояние.
- [x] README переписан под статический запуск (`python3 -m http.server 8000`), package.json синхронизирован с фактическим описанием.
- [x] `UIRenderer` теперь полностью читает данные из `ScheduleManager`, статический `CONFIG.ROUTE_SCHEDULE` используется только как запасной вариант в UI.
- [x] Обновлены `docs/current-state.md` и `docs/tech-debt.md`, отражающие новое разделение обязанностей.

## Что осталось сделать (главные задачи)
- [x] **Перевести движение машины на ScheduleManager**  
   Car/Game/PathBuilder теперь получают маршруты через ScheduleManager; `CONFIG.ROUTE_SCHEDULE` остался только как fallback на случай отсутствия задач:
   - [x] Ввести в Game/helpers методы вида `getCurrentTask()`, `getNextTask()` и использовать их вместо прямого доступа к конфигу.
   - [x] Переписать `Car.saveStateForNextDestination`, `Game.nextDestination`, PathBuilder так, чтобы они работали с задачами, а не с индексами статического расписания.
   - [x] После полной миграции удалить `ROUTE_SCHEDULE` либо оставить минимальный fallback (например, для первого запуска).

- [ ] **Smoke-тесты менеджеров**  
   - Зафиксировать поведение TimeManager при паузе и переключении скорости (reset `lastTimeUpdate` при возобновлении).
   - Добавить базовые тесты/скрипты для ScheduleManager (смена дня, переход задач, stay timer через полночь).

- [ ] **Планируемые расширения**  
   - После выравнивания маршрутов вернуться к игровым событиям (сообщения/друзья) и к UX-улучшениям.

- [ ] **Выровнять TimeManager/DayNight и данные для персонажей**  
   - [x] Доработать TimeManager: хранить `dayOfWeek`, синхронизировать паузы и множители, возвращать чистые данные вместо HTML-строк.
   - [x] Перевести DayNightManager на API TimeManager.
   - [x] Убрать прямое управление PIXI-слоями из DayNightManager.
   - [x] Обновить `Shina` и завязанные системы на новые поля (`dayOfWeek`, консолидированная скорость времени).

- [ ] **Разбить Game на отдельные контроллеры**  
   - Вынести рендеринг, симуляцию и ввод в отдельные модули; отказаться от экспорта `window.*` в пользу контейнера зависимостей.
   - Сфокусировать Game на orchestration-слое, убрав из него прямые обращения к Canvas/PIXI/DOM.

- [ ] **Консолидировать маршрутизацию транспорта**  
   - Перенести построение путей полностью в PathBuilder, чтобы `Car` и `MovementController` не дублировали расчёт.
   - Упростить MovementController: отделить расчёт движения, проверки светофоров и пользовательские колбэки.
   - После упрощения добавить точечные тесты на маршруты и работу со светофорами.

## Дополнительные заметки
- Документация в `docs/` содержит только актуальные файлы. Если появятся новые направления работы — добавлять короткие записки и поддерживать их в актуальном состоянии.
- npm-скрипты — это просто удобные обёртки над `python3 -m http.server`; если появится реальный Node-бэкенд, нужно будет задокументировать его отдельно.
